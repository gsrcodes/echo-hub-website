{
  "name": "unexly-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/out/prints/logo && cp index.html dist/ && cp -r css dist/ && cp -r js dist/ && cp out/prints/logo/logo_unexly.png dist/out/prints/logo/"
  }
}
